import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  subContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 6,
  },
  titleText: {
    fontSize: 16,
    fontWeight: '500',
    color: '#b32d24',
  },
});
